{
  "name": "scraping",
  "nodes": [
    {
      "parameters": {
        "url": "https://api.zenrows.com/v1/",
        "sendQuery": true,
        "queryParameters": {
          "parameters": [
            {
              "name": "apikey",
              "value": "f01f1ca19fd796db31b7961c3e3b5c8f58a83bb4"
            },
            {
              "name": "url",
              "value": "={{ $json.targetUrl }}"
            },
            {
              "name": "js_render",
              "value": "true"
            },
            {
              "name": "premium_proxy",
              "value": "true"
            },
            {
              "name": "antibot",
              "value": "true"
            },
            {
              "name": "wait",
              "value": "3000"
            }
          ]
        },
        "options": {}
      },
      "id": "8c56abc9-5968-439f-8e4a-d80181d1f486",
      "name": "ZenRows (Scraping Antibot)",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.1,
      "position": [
        -672,
        80
      ],
      "continueOnFail": true
    },
    {
      "parameters": {
        "rules": {
          "values": [
            {
              "conditions": {
                "options": {
                  "caseSensitive": true,
                  "leftValue": "",
                  "typeValidation": "strict",
                  "version": 3
                },
                "conditions": [
                  {
                    "leftValue": "={{ $('Loop (Iterar una a una)').item.json.targetUrl }}",
                    "rightValue": "endesa",
                    "operator": {
                      "type": "string",
                      "operation": "contains"
                    },
                    "id": "0d285548-7aa0-470b-8d4f-0b44d1a86813"
                  }
                ],
                "combinator": "and"
              }
            },
            {
              "conditions": {
                "options": {
                  "caseSensitive": true,
                  "leftValue": "",
                  "typeValidation": "strict",
                  "version": 3
                },
                "conditions": [
                  {
                    "id": "3572d065-f468-4ad6-9ec8-59ca8543d672",
                    "leftValue": "={{ $('Loop (Iterar una a una)').item.json.targetUrl }}",
                    "rightValue": "naturgy",
                    "operator": {
                      "type": "string",
                      "operation": "contains"
                    }
                  }
                ],
                "combinator": "and"
              }
            },
            {
              "conditions": {
                "options": {
                  "caseSensitive": true,
                  "leftValue": "",
                  "typeValidation": "strict",
                  "version": 3
                },
                "conditions": [
                  {
                    "id": "0118ba7c-e5fd-4dca-87dd-10cf07306a42",
                    "leftValue": "={{ $('Loop (Iterar una a una)').item.json.targetUrl }}",
                    "rightValue": "digimobil",
                    "operator": {
                      "type": "string",
                      "operation": "contains"
                    }
                  }
                ],
                "combinator": "and"
              }
            },
            {
              "conditions": {
                "options": {
                  "caseSensitive": true,
                  "leftValue": "",
                  "typeValidation": "strict",
                  "version": 3
                },
                "conditions": [
                  {
                    "id": "151d3f1b-a230-401f-b2f6-e9906613cb4d",
                    "leftValue": "={{ $('Loop (Iterar una a una)').item.json.targetUrl }}",
                    "rightValue": "movistar",
                    "operator": {
                      "type": "string",
                      "operation": "contains"
                    }
                  }
                ],
                "combinator": "and"
              }
            },
            {
              "conditions": {
                "options": {
                  "caseSensitive": true,
                  "leftValue": "",
                  "typeValidation": "strict",
                  "version": 3
                },
                "conditions": [
                  {
                    "id": "c5278190-9746-4128-bfb2-4cdef9785c28",
                    "leftValue": "={{ $('Loop (Iterar una a una)').item.json.targetUrl }}",
                    "rightValue": "orange",
                    "operator": {
                      "type": "string",
                      "operation": "contains"
                    }
                  }
                ],
                "combinator": "and"
              }
            }
          ]
        },
        "options": {}
      },
      "type": "n8n-nodes-base.switch",
      "typeVersion": 3.4,
      "position": [
        -448,
        32
      ],
      "id": "d900b5c2-13f2-43be-91ed-609db54562cf",
      "name": "Switch"
    },
    {
      "parameters": {
        "jsCode": "const out = [];\n\n// 1. Obtenemos el HTML (buscamos en diferentes propiedades por si acaso)\nconst htmlText = $input.first().json.data || $input.first().json.texto_limpio || $input.first().json.body || \"\";\n\nif (!htmlText) {\n    return [{ json: { error: \"El HTML de Digi llegó vacío.\" } }];\n}\n\n// 2. PARSER DE DIGI (Fibra + Móvil)\n// Digi envuelve sus combinaciones principales en la clase \"card panelProductHorizontal gaList\"\nconst digiCards = htmlText.split('class=\"card panelProductHorizontal gaList\"');\n\n// Usamos un Set para evitar duplicados en la extracción\nconst vistos = new Set();\n\nfor (let i = 1; i < digiCards.length; i++) {\n    const card = digiCards[i];\n    \n    // Precio total mensual\n    const priceMatch = card.match(/<div class=\"amount\">(\\d+)<\\/div>/);\n    if (!priceMatch) continue;\n    const precio = parseFloat(priceMatch[1]);\n    \n    // Construir el nombre de la tarifa uniendo los productos que contiene la tarjeta\n    const products = [];\n    const productDivs = card.split('product_ga\"');\n    \n    for(let j = 1; j < productDivs.length; j++) {\n        const pDiv = productDivs[j];\n        \n        // Buscamos el nombre base y si tiene multiplicadores (líneas extra)\n        const nameMatch = pDiv.match(/data-name=\"([^\"]+)\"/);\n        const unitMatch = pDiv.match(/data-units=\"([^\"]+)\"/);\n        \n        if (nameMatch) {\n            let name = nameMatch[1].trim();\n            // Si hay más de una línea (ej: 2x IlimiTODO), lo añadimos al nombre\n            if (unitMatch && parseInt(unitMatch[1]) > 1) {\n                name = unitMatch[1] + \"x \" + name;\n            }\n            products.push(name);\n        }\n    }\n    \n    // Ejemplo de salida: \"Fibra SMART 500Mb + 2x IlimiTODO\"\n    const nombre_tarifa = products.join(' + ');\n    \n    if (vistos.has(nombre_tarifa)) continue;\n    vistos.add(nombre_tarifa);\n\n    // Condiciones: Extraemos los textos descriptivos de Fibra y Móvil limpios de HTML\n    let condiciones = [];\n    \n    // Texto de la fibra (ej: Fibra SMART 500 Mbps)\n    const speedMatch = card.match(/<div class=\"speed\">([\\s\\S]*?)<\\/div>/);\n    if (speedMatch) {\n        condiciones.push(\"- \" + speedMatch[1].replace(/<[^>]+>/g, '').replace(/\\s+/g, ' ').trim());\n    }\n    \n    // Texto del móvil (ej: Móvil 50 GB acumulables...)\n    const movilMatch = card.match(/(Móvil\\s*<b>[\\s\\S]*?)<\\/div>/);\n    if (movilMatch) {\n        condiciones.push(\"- \" + movilMatch[1].replace(/<[^>]+>/g, '').replace(/\\s+/g, ' ').trim());\n    }\n\n    // URL de contratación directa\n    let url_oferta = null;\n    const urlMatch = card.match(/href=\"(\\/combina-telefonia-internet[^\"]+)\"/);\n    if (urlMatch) {\n        // Limpiamos los \"&amp;\" a un \"&\" normal\n        url_oferta = \"https://www.digimobil.es\" + urlMatch[1].replace(/&amp;/g, '&');\n    }\n\n    out.push({\n        json: {\n            id_proveedor: 20,\n            id_tipo_servicio:3,\n            nombre_tarifa: nombre_tarifa,\n            precio: precio,\n            precios_adicionales: null, // Digi da un precio cerrado por el combo\n            unidad_precio: \"€/mes\",\n            permanencia: \"3 meses (Fibra) / Sin permanencia (Móvil)\", // Fijo en las políticas de Digi\n            condiciones: condiciones.join('\\n'),\n            url_oferta_externa: url_oferta || $('Loop (Iterar una a una)').first().json.targetUrl\n        }\n    });\n}\n\n// Chivato de seguridad\nif (out.length === 0) {\n    return [{ json: { error: \"El parser se ejecutó, pero no logró encontrar tarifas de Digi. Revisa la estructura.\" } }];\n}\n\nreturn out;"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -224,
        80
      ],
      "id": "a6ced746-4802-4d8c-912d-5a95b04f84f7",
      "name": "Parser Digi"
    },
    {
      "parameters": {
        "jsCode": "const out = [];\n\n// 1. Obtenemos el HTML de la entrada\nconst htmlText = $input.first().json.data || $input.first().json.texto_limpio || $input.first().json.body || \"\";\n\nif (!htmlText) {\n    return [{ json: { error: \"El HTML de Movistar llegó vacío.\" } }];\n}\n\n// 2. Buscamos el \"tesoro\": el objeto JSON que Next.js inyecta en la página\nconst nextDataMatch = htmlText.match(/<script id=\"__NEXT_DATA__\"[^>]*>([\\s\\S]*?)<\\/script>/);\n\nif (nextDataMatch) {\n    try {\n        const jsonData = JSON.parse(nextDataMatch[1]);\n        \n        // Navegamos por el árbol del JSON hasta los packs \"Más vendidos\" (las tarifas armadas)\n        const bestSellers = jsonData.props?.pageProps?.initialState?.recomendator?.data?.recomAndBestSellers?.bestSellers || [];\n\n        bestSellers.forEach(pack => {\n            const rec = pack.recommendation;\n            if (!rec) return;\n\n            // Extraemos precio actual y precio original (por si hay una promoción aplicada)\n            const precioActual = rec[\"cms-atom-price-1\"]?.price || null;\n            const precioSinPromo = rec[\"cms-atom-price-2\"]?.price || null;\n\n            // Extraemos los bloques de texto. Vienen con HTML (<ul>, <li>, <strong>), así que lo limpiamos\n            const texto1 = rec[\"cms-atom-text-1\"]?.text || \"\";\n            const texto2 = rec[\"cms-atom-text-2\"]?.text || \"\";\n            \n            // Función auxiliar para limpiar etiquetas HTML y espacios extra\n            const cleanHTML = (htmlStr) => {\n                return htmlStr\n                    .replace(/<[^>]+>/g, ' ') // Quita etiquetas\n                    .replace(/&nbsp;/g, ' ')  // Quita espacios non-breaking\n                    .replace(/\\s+/g, ' ')     // Colapsa múltiples espacios\n                    .trim();\n            };\n\n            const descripcionComunicaciones = cleanHTML(texto1); // Info de Fibra y Móvil\n            const descripcionTVDispositivos = cleanHTML(texto2); // Info de TV y Dispositivos\n            \n            // Extraemos permanencia y la URL de contratación/info\n            const permanencia = rec[\"cms-atom-text-4\"]?.text || \"Consultar condiciones\";\n            let url = rec[\"cms-atom-button-2\"]?.href || null;\n            \n            // Movistar a veces usa URLs relativas internas para sus modales, las formateamos bien\n            if (url && url.includes(\"cms-modal-page:\")) {\n                url = url.replace(\"cms-modal-page:\", \"https://www.movistar.es\");\n                // Limpiamos los parámetros internos del modal para dejar una URL limpia\n                url = url.split(\"?\")[0]; \n            }\n\n            out.push({\n                json: {\n                    id_proveedor: 5,\n                    id_tipo_servicio:3,\n                    nombre_tarifa: \"Pack miMovistar\", // Movistar usa combinaciones modulares bajo este nombre\n                    descripcion_comunicaciones: descripcionComunicaciones,\n                    descripcion_tv_y_dispositivos: descripcionTVDispositivos,\n                    precio: precioActual,\n                    precio_sin_promo: precioSinPromo !== \"\" ? precioSinPromo : null,\n                    unidad_precio: \"€/mes\",\n                    permanencia: permanencia,\n                    url_oferta_externa: url || $('Loop (Iterar una a una)').first().json.targetUrl\n                }\n            });\n        });\n\n    } catch(e) {\n        return [{ json: { error: \"Error al parsear el JSON interno de Movistar: \" + e.message } }];\n    }\n} else {\n    return [{ json: { error: \"No se encontró el bloque __NEXT_DATA__ en el HTML de Movistar.\" } }];\n}\n\n// Chivato de seguridad\nif (out.length === 0) {\n    return [{ json: { error: \"El script se ejecutó, pero no encontró packs configurados en los datos de Movistar.\" } }];\n}\n\nreturn out;"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -224,
        272
      ],
      "id": "a70cbfc8-6d69-403d-8b30-af27646897ab",
      "name": "Parser Movistar"
    },
    {
      "parameters": {
        "jsCode": "const out = [];\n\n// 1. Obtenemos el HTML de la entrada\nconst htmlText = $input.first().json.data || $input.first().json.texto_limpio || $input.first().json.body || \"\";\n\nif (!htmlText) {\n    return [{ json: { error: \"El HTML de Orange llegó vacío.\" } }];\n}\n\n// 2. PARSER DE ORANGE\n// Orange usa la clase \"card-tariff-new\" para sus contenedores de tarifas\nconst blocks = htmlText.split('class=\"card-tariff-new');\n\nfor (let i = 1; i < blocks.length; i++) {\n    const block = blocks[i];\n\n    // Extraer datos del input hidden (Nombre, ID y Precio base)\n    const dataMatch = block.match(/dataSimpleTariff[^>]*data-price=\"([^\"]+)\"[^>]*data-name=\"([^\"]+)\"/);\n    if (!dataMatch) continue;\n\n    const precioBase = dataMatch[1];\n    const nombreTecnico = dataMatch[2];\n\n    // Extraer el Título comercial (Ej: Cine y Series con 1 línea)\n    const titleMatch = block.match(/card-tariff-new__header[^>]*>\\s*([\\s\\S]*?)\\s*<\\/div>/);\n    const nombreComercial = titleMatch ? titleMatch[1].trim() : nombreTecnico;\n\n    // Extraer características (Fibra, Móvil, TV)\n    let condiciones = [];\n    const featureRegex = /card-tariff-new__features__main[^>]*>\\s*([\\s\\S]*?)\\s*<\\/div>/g;\n    let featureM;\n    while ((featureM = featureRegex.exec(block)) !== null) {\n        let text = featureM[1].replace(/<[^>]+>/g, '').replace(/\\s+/g, ' ').trim();\n        if (text) condiciones.push(\"- \" + text);\n    }\n\n    // Extraer servicios de streaming incluidos (Netflix, Disney, etc.)\n    const streamingMatch = block.match(/Incluído 1 servicio streaming/);\n    if (streamingMatch) {\n        // Buscamos iconos que NO tengan la clase \"disabled\"\n        if (block.includes('iconassets__disney-plus') && !block.includes('card-tariff-simple__features__item--disabled\"> <hl-icon name=\"iconassets__disney-plus')) {\n            condiciones.push(\"- Incluye Disney+\");\n        }\n        if (block.includes('iconassets__netflix-mini') && !block.includes('card-tariff-simple__features__item--disabled\"> <hl-icon name=\"iconassets__netflix-mini')) {\n            condiciones.push(\"- Incluye Netflix\");\n        }\n    }\n\n    // Extraer Precio Promocional (si existe)\n    // Orange usa el componente <hl-price-block> para los precios finales\n    const promoPriceMatch = block.match(/unit=\"(\\d+)\"[^>]*size=\"md\"/);\n    const precioFinal = promoPriceMatch ? promoPriceMatch[1] : precioBase;\n\n    // Extraer URL de \"Más info\"\n    let url_oferta = null;\n    const urlMatch = block.match(/href=\"([^\"]+)\"[^>]*class=\"[^\"]*js-more-info-tariff/);\n    if (urlMatch) {\n        url_oferta = urlMatch[1].startsWith('http') ? urlMatch[1] : \"https://www.orange.es\" + urlMatch[1];\n    }\n\n    out.push({\n        json: {\n            id_proveedor: 14,\n            id_tipo_servicio:3,\n            nombre_tarifa: nombreComercial,\n            precio: parseFloat(precioFinal),\n            precio_fuera_promo: parseFloat(precioBase),\n            unidad_precio: \"€/mes\",\n            permanencia: block.toLowerCase().includes(\"sin permanencia\") ? \"Sin permanencia\" : \"12 meses\",\n            condiciones: condiciones.join('\\n'),\n            url_oferta_externa: url_oferta || $('Loop (Iterar una a una)').first().json.targetUrl\n        }\n    });\n}\n\n// Limpiar duplicados por nombre de tarifa\nconst uniqueOut = Array.from(new Map(out.map(item => [item.json.nombre_tarifa, item])).values());\n\nif (uniqueOut.length === 0) {\n    return [{ json: { error: \"No se detectaron tarifas de Orange. Revisa si el HTML contiene la clase 'card-tariff-new'.\" } }];\n}\n\nreturn uniqueOut;"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -224,
        464
      ],
      "id": "41d8a046-31d7-4d20-a07c-5202729c1098",
      "name": "Parser Orange"
    },
    {
      "parameters": {
        "jsCode": "const items = $input.all();\nconst out = [];\n\nfor (const item of items) {\n  // El HTML limpio que recibimos de ZenRows\n  const htmlText = item.json.data || item.json.body || item.json.response || item.json.texto_limpio;\n  \n  if (!htmlText) continue;\n\n  // Dividimos el HTML gigante en \"trozos\", cada uno representando una tarjeta de tarifa\n  // Endesa guarda el nombre de la tarifa en el atributo data-product-name\n  const cards = htmlText.split('data-product-name=\"');\n\n  // Ignoramos el primer trozo (que es la cabecera de la web)\n  for (let i = 1; i < cards.length; i++) {\n    const cardHtml = cards[i];\n\n    // 1. EXTRAER NOMBRE TARIFA\n    const nombre_tarifa = cardHtml.substring(0, cardHtml.indexOf('\"')).trim();\n\n    // 2 y 3. EXTRAER PRECIO Y UNIDAD (Buscamos el primer precio destacado)\n    let precio = null;\n    let unidad_precio = null;\n\n    const priceBlockMatch = cardHtml.match(/class=\"[^\"]*price__detail__price__discounted[^>]*>([\\s\\S]*?)<\\/p>/);\n    if (priceBlockMatch) {\n        const priceValMatch = priceBlockMatch[1].match(/<span class=\"price\">\\s*([\\d,.]+)\\s*<\\/span>/);\n        const unitValMatch = priceBlockMatch[1].match(/<span class=\"type\">\\s*([^<]+)\\s*<\\/span>/);\n\n        if (priceValMatch) {\n            // Endesa usa comas para los decimales (ej. 0,126600). Lo pasamos a punto para MySQL\n            let cleaned = priceValMatch[1].replace(/[^0-9,]/g, '').replace(',', '.');\n            precio = parseFloat(cleaned);\n        }\n        if (unitValMatch) {\n            unidad_precio = unitValMatch[1].trim();\n        }\n    }\n\n    // 4 y 5. EXTRAER PERMANENCIA Y CONDICIONES (Listado de ventajas)\n    let permanencia = \"Consultar condiciones\"; // Valor por defecto\n    let condicionesList = [];\n    \n    // Buscamos todos los textos de la lista de características de esa tarifa\n    const listItemsMatches = cardHtml.matchAll(/<p class=\"[^\"]*list__item__text\">([\\s\\S]*?)<\\/p>/g);\n    for (const match of listItemsMatches) {\n        let text = match[1].trim();\n        condicionesList.push(\"- \" + text);\n        // Si alguna viñeta habla de permanencia, la guardamos en su campo específico\n        if (text.toLowerCase().includes(\"permanencia\")) {\n            permanencia = text;\n        }\n    }\n\n    // Extraemos también la \"letra pequeña\" y notas legales que Endesa oculta en el data-fullnote\n    const noteMatch = cardHtml.match(/data-fullnote=\"([^\"]+)\"/);\n    if (noteMatch) {\n        // Limpiamos los tags HTML que Endesa mete dentro de ese atributo (&lt;strong&gt; etc)\n        let nota = noteMatch[1].replace(/&lt;/g, '<').replace(/&gt;/g, '>').replace(/&nbsp;/g, ' ');\n        nota = nota.replace(/<[^>]+>/g, ''); \n        condicionesList.push(\"\\nLegal: \" + nota);\n    }\n    const condiciones = condicionesList.join('\\n');\n\n    // 6. EXTRAER URL DE LA OFERTA EXTERNA\n    let url_oferta_externa = null;\n    const urlMatch = cardHtml.match(/<a[^>]+href=\"([^\"]+)\"[^>]*class=\"[^\"]*button__primary[^\"]*\"/i);\n    if (urlMatch) {\n        let extractedUrl = urlMatch[1].trim();\n        // Si el enlace es relativo (ej. /es/luz...), le añadimos el dominio principal\n        if (extractedUrl.startsWith('/')) {\n            extractedUrl = \"https://www.endesa.com\" + extractedUrl;\n        }\n        url_oferta_externa = extractedUrl;\n    }\n\n    // Filtro de calidad: Solo enviamos a MySQL aquellas tarjetas donde sí hemos encontrado un precio.\n    if (nombre_tarifa && precio !== null) {\n        out.push({\n            json: {\n                id_proveedor: 1,\n                id_tipo_servicio:1,\n                nombre_tarifa: nombre_tarifa,\n                precio: precio,\n                unidad_precio: unidad_precio,\n                permanencia: permanencia,\n                condiciones: condiciones,\n                url_oferta_externa: url_oferta_externa || $('Loop (Iterar una a una)').first().json.targetUrl\n               \n            }\n        });\n    }\n  }\n}\n\nreturn out;"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -224,
        -304
      ],
      "id": "c5885718-881c-4071-8b56-00a8e2494c61",
      "name": "Parser Endesa  "
    },
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "daysInterval": 5
            }
          ]
        }
      },
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.3,
      "position": [
        -1584,
        64
      ],
      "id": "7928e06f-0cb1-4527-83ee-f5a659e3ae62",
      "name": "Schedule Trigger"
    },
    {
      "parameters": {
        "operation": "upsert",
        "table": {
          "__rl": true,
          "value": "tarifas",
          "mode": "list",
          "cachedResultName": "tarifas"
        },
        "dataMode": "defineBelow",
        "columnToMatchOn": "nombre_tarifa",
        "valueToMatchOn": "={{ $json.nombre_tarifa }}",
        "valuesToSend": {
          "values": [
            {
              "column": "precio",
              "value": "={{ $json.precio }}"
            },
            {
              "column": "permanencia",
              "value": "={{ $json.permanencia }}"
            },
            {
              "column": "condiciones",
              "value": "={{ $json.condiciones }}"
            },
            {
              "column": "url_oferta_externa",
              "value": "={{ $json.url_oferta_externa }}"
            },
            {
              "column": "id_servicio",
              "value": "={{ $json.id_servicio }}"
            }
          ]
        },
        "options": {}
      },
      "id": "39a717da-95c2-48f0-82ba-9c90f9e22941",
      "name": "MySQL (Guardar Datos en tarifas)",
      "type": "n8n-nodes-base.mySql",
      "typeVersion": 2.1,
      "position": [
        752,
        304
      ],
      "alwaysOutputData": true,
      "credentials": {
        "mySql": {
          "id": "Nl5gkbRXGYGvAiPU",
          "name": "MySQL account"
        }
      }
    },
    {
      "parameters": {
        "mode": "combine",
        "combineBy": "combineByPosition",
        "numberInputs": 3,
        "options": {}
      },
      "type": "n8n-nodes-base.merge",
      "typeVersion": 3.2,
      "position": [
        544,
        208
      ],
      "id": "1cee7bf8-9024-4a38-8ba9-ae86488a0ad2",
      "name": "Merge"
    },
    {
      "parameters": {
        "jsCode": "const out = [];\n\n// 1. Obtenemos el HTML de la entrada\nconst htmlText = $input.first().json.data || $input.first().json.texto_limpio || $input.first().json.body || \"\";\n\nif (!htmlText) {\n    return [{ json: { error: \"El HTML de Naturgy llegó vacío.\" } }];\n}\n\n// 2. PASO A: Extraer las condiciones de las tarjetas \"shadow-card\"\nconst cardBlocks = htmlText.split('shadow-card__content');\nconst condicionesMap = {};\n\nfor(let i = 1; i < cardBlocks.length; i++) {\n    const block = cardBlocks[i];\n    \n    // Sacamos el título (Ej: Tarifa Por Uso Luz)\n    const titleMatch = block.match(/shadow-card__title[^>]*>\\s*(.*?)\\s*<\\/p>/);\n    if (!titleMatch) continue;\n    const title = titleMatch[1].trim();\n\n    // Sacamos los 'bullet points' de la descripción\n    let condiciones = [];\n    const ulMatch = block.match(/<div class=\"shadow-card__description\"[^>]*>([\\s\\S]*?)<\\/div>/);\n    if (ulMatch) {\n        const liRegex = /<li>(.*?)<\\/li>/g;\n        let liM;\n        while((liM = liRegex.exec(ulMatch[1])) !== null) {\n            // Limpiamos etiquetas HTML y el molesto &nbsp;\n            let text = liM[1].replace(/&nbsp;/g, ' ').replace(/<[^>]+>/g, '').replace(/\\s+/g, ' ').trim();\n            if(text) condiciones.push(\"- \" + text);\n        }\n    }\n    condicionesMap[title] = condiciones;\n}\n\n// 3. PASO B: Extraer precios y URLs de los bloques \"exp229\"\nconst expBlocks = htmlText.split('<div class=\"exp229');\nconst vistos = new Set();\n\nfor (let i = 1; i < expBlocks.length; i++) {\n    const block = expBlocks[i];\n    \n    const titleMatch = block.match(/<p class=\"titulo\"><b>(.*?)<\\/b><\\/p>/);\n    if (!titleMatch) continue;\n    \n    // Naturgy a veces pone \"Por Uso Luz\" en el bloque de precio, pero \"Tarifa Por Uso Luz\" en la tarjeta. \n    // Lo unificamos para que haga match.\n    let nombre_tarifa = titleMatch[1].trim();\n    if (!nombre_tarifa.toLowerCase().includes(\"tarifa\")) {\n        nombre_tarifa = \"Tarifa \" + nombre_tarifa;\n    }\n\n    // Evitamos duplicados (Naturgy repite las tarjetas en versión móvil y desktop)\n    if (vistos.has(nombre_tarifa)) continue;\n    vistos.add(nombre_tarifa);\n\n    let precio = null;\n    let preciosAdicionales = null;\n\n    // Buscamos matemáticamente el precio según el tipo de tarifa\n    if (block.startsWith(' uso\"')) {\n        // Tarifa por uso (ej: 0,<span class=\"no-imp\">109900 </span>)\n        const pMatch = block.match(/0,<span class=\"no-imp\">(\\d+)\\s*<\\/span>/);\n        if (pMatch) precio = parseFloat(\"0.\" + pMatch[1]);\n        \n    } else if (block.startsWith(' noche\"')) {\n        // Tarifa Noche (Valle, Llano, Punta)\n        const arr = [];\n        const pRegex = /<p class=\"precio\">(Valle|Llano|Punta)[\\s\\S]*?<span class=\"no-imp\">([\\d,]+)\\s*<\\/span>/g;\n        let pM;\n        while((pM = pRegex.exec(block)) !== null) {\n            let tipo = pM[1];\n            let val = parseFloat(pM[2].replace(',', '.')); // Convertimos la coma europea a punto\n            arr.push(`${tipo}: ${val}`);\n            if (precio === null) precio = val; // Usamos el primer precio que encuentre como variable principal\n        }\n        if (arr.length > 0) preciosAdicionales = arr.join(' / ');\n        \n    } else if (block.startsWith(' plana\"')) {\n        // Tarifa Plana no tiene precio por kWh fijo, depende del cliente\n        preciosAdicionales = \"Cuota fija personalizada\";\n    }\n\n    // Extraemos el enlace de la oferta\n    let url_oferta = null;\n    const urlMatch = block.match(/href=\"([^\"]+)\" class=\"mas-info\"/);\n    if (urlMatch) {\n        url_oferta = urlMatch[1].startsWith('http') ? urlMatch[1] : \"https://www.naturgy.es\" + urlMatch[1];\n    }\n\n    // Recuperamos las condiciones que parseamos en el paso A\n    let condicionesFinales = condicionesMap[nombre_tarifa] || [];\n\n    // 4. CONSTRUIMOS LA SALIDA\n    out.push({\n        json: {\n            id_proveedor : 4,\n            id_tipo_servicio:2,\n            nombre_tarifa: nombre_tarifa,\n            precio: precio,\n            precios_adicionales: preciosAdicionales,\n            unidad_precio: \"€/kWh\",\n            permanencia: condicionesFinales.join(' ').toLowerCase().includes('sin permanencia') ? 'Sin permanencia' : 'Consultar condiciones',\n            condiciones: condicionesFinales.join('\\n'),\n            url_oferta_externa: url_oferta || $('Loop (Iterar una a una)').first().json.targetUrl\n        }\n    });\n}\n\nif (out.length === 0) {\n    return [{ json: { error: \"No se encontraron tarifas. Revisa la estructura del HTML.\" } }];\n}\n\nreturn out;"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -224,
        -112
      ],
      "id": "36052f73-649d-42fe-b03f-0c7b4f05c420",
      "name": "Parser Naturgy Luz"
    },
    {
      "parameters": {
        "operation": "upsert",
        "table": {
          "__rl": true,
          "value": "servicios",
          "mode": "list",
          "cachedResultName": "servicios"
        },
        "dataMode": "defineBelow",
        "columnToMatchOn": "nombre_servicio",
        "valueToMatchOn": "={{ $json.nombre_tarifa }}",
        "valuesToSend": {
          "values": [
            {
              "column": "id_proveedor",
              "value": "={{ $json.id_proveedor }}"
            },
            {
              "column": "id_tipo_servicio",
              "value": "={{ $json.id_tipo_servicio }}"
            }
          ]
        },
        "options": {}
      },
      "id": "0b9c0197-2361-4db9-8e85-19922049d92b",
      "name": "MySQL (Guardar Datos en sevicios)",
      "type": "n8n-nodes-base.mySql",
      "typeVersion": 2.1,
      "position": [
        272,
        -304
      ],
      "alwaysOutputData": true,
      "credentials": {
        "mySql": {
          "id": "Nl5gkbRXGYGvAiPU",
          "name": "MySQL account"
        }
      }
    },
    {
      "parameters": {
        "operation": "select",
        "table": {
          "__rl": true,
          "value": "servicios",
          "mode": "list",
          "cachedResultName": "servicios"
        },
        "limit": 1,
        "where": {
          "values": [
            {
              "column": "nombre_servicio",
              "value": "={{ $json.nombre_tarifa }}"
            }
          ]
        },
        "sort": {
          "values": []
        },
        "options": {}
      },
      "id": "4f7d17ac-d4e5-4115-8251-15f840665d3c",
      "name": "MySQL (Obtener Id)",
      "type": "n8n-nodes-base.mySql",
      "typeVersion": 2.1,
      "position": [
        240,
        -96
      ],
      "alwaysOutputData": true,
      "executeOnce": false,
      "credentials": {
        "mySql": {
          "id": "Nl5gkbRXGYGvAiPU",
          "name": "MySQL account"
        }
      }
    },
    {
      "parameters": {
        "amount": 1
      },
      "id": "bc9b9715-6c13-4819-883a-8e641ae8148c",
      "name": "Wait (Anti-Rate Limit)",
      "type": "n8n-nodes-base.wait",
      "typeVersion": 1.1,
      "position": [
        976,
        544
      ],
      "webhookId": "d6320e0c-ad2d-40b3-81bf-bd20e44fee4f"
    },
    {
      "parameters": {
        "options": {}
      },
      "id": "abe0fd7a-beef-4f27-a3a8-ce4e3389e8e5",
      "name": "Loop (Iterar una a una)",
      "type": "n8n-nodes-base.splitInBatches",
      "typeVersion": 3,
      "position": [
        -992,
        64
      ]
    },
    {
      "parameters": {
        "jsCode": "//const postalCodes = ['18005', '18014', '18002', '18006', '18007', '18004', '18003', '18001'];\nconst postalCodes = ['18005'];\nconst companies = [\n  \n  \n  //Funcionales\n\n  { name: 'Orange', url: 'https://www.orange.es/tarifas' },\n  { name: 'Movistar', url: 'https://www.movistar.es/fibra-y-movil/' },\n  { name: 'Digi', url: 'https://www.digimobil.es/fibra-movil' },\n  { name: 'Naturgy', url: 'https://www.naturgy.es/hogar/luz' },\n  //{ name: 'Naturgy', url: 'https://www.naturgy.es/hogar/gas' },\n  { name: 'Endesa', url: 'https://www.endesa.com/es/luz-y-gas/luz' },\n  //{ name: 'Endesa', url: 'https://www.endesa.com/es/luz-y-gas/gas'},\n  //{ name: 'Endesa', url: 'https://www.endesa.com/es/luz-y-gas/luz-gas'},\n];\n\nconst items = [];\n\nfor (const cp of postalCodes) {\n  for (const company of companies) {\n    items.push({\n      json: {\n        codigoPostal: cp,\n        company: company.name,\n        targetUrl: company.url\n      }\n    });\n  }\n}\n\nreturn items;"
      },
      "id": "5caecdf2-c1ad-4d53-9dfa-21a577f9de66",
      "name": "Generar Tareas (Compañías)",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -1360,
        64
      ]
    }
  ],
  "pinData": {},
  "connections": {
    "ZenRows (Scraping Antibot)": {
      "main": [
        [
          {
            "node": "Switch",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Switch": {
      "main": [
        [
          {
            "node": "Parser Endesa  ",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Parser Naturgy Luz",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Parser Digi",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Parser Movistar",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Parser Orange",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Parser Digi": {
      "main": [
        [
          {
            "node": "MySQL (Guardar Datos en sevicios)",
            "type": "main",
            "index": 0
          },
          {
            "node": "MySQL (Obtener Id)",
            "type": "main",
            "index": 0
          },
          {
            "node": "Merge",
            "type": "main",
            "index": 2
          }
        ]
      ]
    },
    "Parser Movistar": {
      "main": [
        [
          {
            "node": "MySQL (Guardar Datos en sevicios)",
            "type": "main",
            "index": 0
          },
          {
            "node": "MySQL (Obtener Id)",
            "type": "main",
            "index": 0
          },
          {
            "node": "Merge",
            "type": "main",
            "index": 2
          }
        ]
      ]
    },
    "Parser Orange": {
      "main": [
        [
          {
            "node": "MySQL (Obtener Id)",
            "type": "main",
            "index": 0
          },
          {
            "node": "MySQL (Guardar Datos en sevicios)",
            "type": "main",
            "index": 0
          },
          {
            "node": "Merge",
            "type": "main",
            "index": 2
          }
        ]
      ]
    },
    "Parser Endesa  ": {
      "main": [
        [
          {
            "node": "MySQL (Guardar Datos en sevicios)",
            "type": "main",
            "index": 0
          },
          {
            "node": "MySQL (Obtener Id)",
            "type": "main",
            "index": 0
          },
          {
            "node": "Merge",
            "type": "main",
            "index": 2
          }
        ]
      ]
    },
    "Schedule Trigger": {
      "main": [
        [
          {
            "node": "Generar Tareas (Compañías)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Merge": {
      "main": [
        [
          {
            "node": "MySQL (Guardar Datos en tarifas)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "MySQL (Guardar Datos en tarifas)": {
      "main": [
        [
          {
            "node": "Wait (Anti-Rate Limit)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Parser Naturgy Luz": {
      "main": [
        [
          {
            "node": "MySQL (Guardar Datos en sevicios)",
            "type": "main",
            "index": 0
          },
          {
            "node": "MySQL (Obtener Id)",
            "type": "main",
            "index": 0
          },
          {
            "node": "Merge",
            "type": "main",
            "index": 2
          }
        ]
      ]
    },
    "MySQL (Guardar Datos en sevicios)": {
      "main": [
        [
          {
            "node": "Merge",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "MySQL (Obtener Id)": {
      "main": [
        [
          {
            "node": "Merge",
            "type": "main",
            "index": 1
          }
        ]
      ]
    },
    "Wait (Anti-Rate Limit)": {
      "main": [
        [
          {
            "node": "Loop (Iterar una a una)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Loop (Iterar una a una)": {
      "main": [
        [],
        [
          {
            "node": "ZenRows (Scraping Antibot)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Generar Tareas (Compañías)": {
      "main": [
        [
          {
            "node": "Loop (Iterar una a una)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1",
    "binaryMode": "separate"
  },
  "versionId": "beb9f353-870f-44e9-a0dc-7a9b68ee8bfe",
  "meta": {
    "templateCredsSetupCompleted": true,
    "instanceId": "8d11eec518c1ec77f3998b7f2a37ee8ebdf17d2060bd2ebaebd07ab69975021c"
  },
  "id": "lKibDMr5DwNUeSYb",
  "tags": []
}
